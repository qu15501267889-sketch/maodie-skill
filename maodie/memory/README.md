# 记忆系统目录

> 此目录下的文件为本地存储，不会上传到 GitHub。

- `sessions/` — 对话记录（重大事件自动写入 + 对话结束时完整摘要）
- `patterns.md` — 模式识别记录（跨会话自动维护）
- `timeline.md` — 关键事件时间线（跨会话自动维护）
- `user_profile.md` — 用户基本信息

## 工作方式

每次对话开始，🐱🐱自动读取这些文件。
对话中：出现重大转折/新信息/核心洞察时自动写入。
对话结束：写完整摘要，末尾追加"🐱🐱记下了"。